name: title_rate_quote_contract_proof

on:
  pull_request:
    paths:
      - "tests/contracts/test_title_rate_quote_contract.py"
      - "docs/contracts/TITLE_RATE_PROVIDER_ADAPTER_CONTRACT_V1.md"
      - "docs/contracts/PRICE_ENGINE_CONTRACT_COVERAGE_INDEX_V1.md"
      - "docs/contracts/fixtures/title_rate_quote/**"
      - "docs/architecture/ROUTE_MAP_V1.md"
      - "azure/functions/asset_ingest/title_rate_handler.py"
      - "azure/functions/asset_ingest/title_rate_provider.py"
      - ".github/workflows/title_rate_quote_contract_proof.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  title-rate-quote-contract-proof:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run title rate quote contract proof
        shell: bash
        run: |
          set -euo pipefail
          python -m unittest discover -s tests/contracts -p 'test_title_rate_quote_contract.py' -v

      - name: Write contract proof summary
        shell: bash
        run: |
          {
            echo "TITLE RATE QUOTE CONTRACT PROOF"
            echo "- test module: tests/contracts/test_title_rate_quote_contract.py"
            echo "- result: pass"
            echo "- scope: POST /api/price-engine/title-rate-quote"
          } >> "$GITHUB_STEP_SUMMARY"
