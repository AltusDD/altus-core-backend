name: ecc_portfolio_summary_contract_proof

on:
  pull_request:
    paths:
      - "tests/contracts/test_ecc_portfolio_summary_contract.py"
      - "docs/contracts/ECC_PORTFOLIO_SUMMARY_CONTRACT_V1.md"
      - "docs/contracts/fixtures/ecc_portfolio_summary/**"
      - "azure/functions/asset_ingest/ecc_portfolio_summary_handler.py"
      - "azure/functions/asset_ingest/ecc_portfolio_summary_service.py"
      - ".github/workflows/ecc_portfolio_summary_contract_proof.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ecc-portfolio-summary-contract-proof:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run ECC portfolio summary contract proof
        shell: bash
        run: |
          set -euo pipefail
          python -m unittest discover -s tests/contracts -p 'test_ecc_portfolio_summary_contract.py' -v

      - name: Write contract proof summary
        shell: bash
        run: |
          {
            echo "ECC PORTFOLIO SUMMARY CONTRACT PROOF"
            echo "- test module: tests/contracts/test_ecc_portfolio_summary_contract.py"
            echo "- result: pass"
            echo "- scope: GET /api/ecc/portfolio/summary"
          } >> "$GITHUB_STEP_SUMMARY"
